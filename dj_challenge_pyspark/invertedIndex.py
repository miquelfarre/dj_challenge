from pyspark import SparkContext
from hdfs import Config

if __name__ == "__main__":    
    print("hdfs python lib to read hadoop directory file, will be processed next, ")
    print("then code logic should be the same than scala module.")
