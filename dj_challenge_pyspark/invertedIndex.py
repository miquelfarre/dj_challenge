from pyspark import SparkContext
from hdfs import Config

if __name__ == "__main__":    

println("hdfs python lib to read hadoop directory file, will be processed next, ")
println("then code logic should be the same than scala module.")